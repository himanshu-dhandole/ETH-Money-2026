# Deployment Configuration
PRIVATE_KEY=your_private_key_here

# Network RPC URLs
RPC_URL=https://rpc.testnet.arc.network

# Native Arc USDC (6 decimals) - DO NOT CHANGE
# This is the official USDC address on Arc testnet
USDC_ADDRESS=0x3600000000000000000000000000000000000000

# YieldReserve Address (set after deploying YieldReserve)
# Deploy YieldReserve ONCE using: forge script script/DeployYieldReserve.s.sol:DeployYieldReserve --broadcast
YIELD_RESERVE_ADDRESS=

# Optional: Custom reserve funding amount in USDC (6 decimals)
# Default: 1000 USDC = 1000000000 (1000 * 10^6)
RESERVE_FUND_AMOUNT=1000000000
